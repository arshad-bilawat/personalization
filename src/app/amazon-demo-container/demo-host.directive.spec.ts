import { DemoHostDirective } from './demo-host.directive';

describe('DemoHostDirective', () => {
  it('should create an instance', () => {
    const directive = new DemoHostDirective();
    expect(directive).toBeTruthy();
  });
});
